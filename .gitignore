node_modules

# Local Netlify folder
.netlify

# Environment variables — never commit secrets
.env
.env.local
.env*.local

# Import/data scripts
import_2025.py
fix_departments_2025.py
query_santa_ana.py
query_dept.py
*.xlsx
municipalities.json
Municipalities.csv

# Build output — generado por Netlify en el pipeline, no debe trackearse
build/

# Grafo de conocimiento auto-generado (skill graphify), regenerable con `graphify update .`
graphify-out/

# Salida de test runs (Playwright/otros), no forma parte del código fuente
test-results/
